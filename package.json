{
  "name": "proxy-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "echo 'Building proxy server...' && mkdir -p dist && cp server.js dist/",
    "start": "node dist/server.js",
    "prestart": "npm run build"
  },
  "dependencies": {
    "axios": "^1.7.3",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
