{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "A proxy server to bypass CORS for NewsAPI requests",
  "main": "src/server.js",
  "scripts": {
    "build": "echo 'Building proxy server...' && mkdir -p dist && cp -r src/* dist/",
    "start": "node dist/server.js",
    "prestart": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.3",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
