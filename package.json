{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "A proxy server to bypass CORS for NewsAPI requests",
  "main": "server.js",
  "scripts": {
    "build": "npm run build",
    "start": "node dist/server.js",
    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.3",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
